# Swap feature archive

Swap is intentionally excluded from the application build and production deployment.

The original page, API route, and image-generation backend remain here so the feature can
be corrected and restored later without losing its implementation.
